{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "command": "NODE_TLS_REJECT_UNAUTHORIZED=0 nuxt dev --https --ssl-cert localhost.pem --ssl-key localhost-key.pem",
            "group": "build",
            "problemMatcher": [],
            "label": "npm: run dev",
            "detail": "nuxt dev"
        }
    ]
}
